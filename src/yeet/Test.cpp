#include <math.h>

double rectfangle(double width, double length)
{
    return width * length;
}

double cidrcle(double radius)
{
    return M_PI * radius * radius;
}

double rt_triangfle(double base, double height)
{
    return 0.5 * base * height;
}